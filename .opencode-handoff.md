# .opencode-handoff — E-Commerce Rock Merch & Roll

**Rama actual:** `feature/contact-page`
**Última actualización:** 2026-07-19

## Estado — Feature: Página de Contacto ✅

### Fase 1 — Backend: API de contacto ✅
- Tabla `contact_messages` con columnas: `id`, `name`, `email`, `phone`, `area`, `subject`, `message`, `created_at`
- `POST /api/contact` — crea mensaje con validaciones inline (nombre >= 2 chars, email regex, asunto >= 3 chars, mensaje >= 10 y <= 2000 chars)
- Áreas de contacto: `ventas`, `soporte`, `reclamos`, `prensa`, `otros`
- Transporter mock (consola) + real (nodemailer SMTP con variables de entorno)
- 7 tests backend en `server/__tests__/routes/contact.test.ts`

### Fase 2 — Servicio + Hook de contacto ✅
- Tipos `ContactFormData`, `ContactResponse`, `ContactAreaInfo`, `ContactArea` en `src/types/contact.ts`
- `sendContactMessage()` en `src/services/contactService.ts` — POST a `/api/contact` con manejo de errores HTTP y de red
- `getContactAreas()` — devuelve lista de áreas
- `useContact()` hook en `src/hooks/useContact.ts` — estado loading/success/error/messageId, submit() y reset()
- 14 tests en `src/__tests__/contact.test.js` (servicio + hook)

### Fase 3 — UI de Contacto ✅
- `ContactPage.tsx` — página completa con formulario, validación inline, pantalla de éxito/error, feedback con useToast
- 6 campos: Nombre, Email, Teléfono, Área (select), Asunto, Mensaje (textarea con contador /2000)
- Diseño responsive mobile-first con grid sm:grid-cols-2, info de contacto en bg-gray-50
- Botones: "Enviar mensaje" (con loading state), "Cancelar", y en éxito: "Volver al inicio" + "Enviar otro mensaje"
- 20 tests en `src/__tests__/ContactPage.test.tsx` (componente mockeado)

### Fase 4 — Routing, integración y tests finales ✅
- Ruta `/contact` en Router.tsx — renderiza ContactPage directamente
- Botón "Contacto" en Header.tsx navega a `/contact` via `navigate('/contact')`
- Tests de navegación en `navigation.test.jsx` (clic en Contacto → /contact)
- 5 tests E2E en `e2e/contact.spec.js` (navegación, formulario válido, validación vacío, email inválido, header navigation)
- Fixes a tests E2E pre-existentes: URL patterns, selectores de precio/talle, navegación por imagen

## Suite de tests total

| Suite | Archivos | Tests | Estado |
|-------|----------|-------|--------|
| Unitarios Frontend (Vitest) | 15 | 260 | ✅ |
| Unitarios Backend (Vitest) | 4 | 22 | ✅ |
| E2E (Playwright) | 8 | 36 | ✅ |
| **Total** | **27** | **318** | ✅ |

## Commits pendientes
1. `feat: agregar ruta /contact en Router y navegación desde Header`
2. `test: tests unitarios de ContactPage`
3. `test: tests E2E de flujo de contacto`
4. `chore: actualizar handoff con feature contacto completa`

## Nuevos archivos creados
- `src/components/contact/ContactPage.tsx` — Componente de página de contacto
- `src/hooks/useContact.ts` — Hook de estado de envío
- `src/services/contactService.ts` — Servicio de contacto
- `src/types/contact.ts` — Tipos de contacto
- `src/__tests__/ContactPage.test.tsx` — Tests de componente
- `src/__tests__/contact.test.js` — Tests de servicio + hook
- `e2e/contact.spec.js` — Tests E2E de contacto
- `server/src/routes/contact.ts` — Ruta POST /api/contact backend
- `server/__tests__/routes/contact.test.ts` — Tests backend de contacto

## Archivos modificados
- `src/components/router/Router.tsx` — Ruta /contact + import ContactPage
- `src/components/layout/Header.tsx` — navigate('/contact') en botón Contacto
- `src/__tests__/navigation.test.jsx` — Tests de navegación a contacto
- `server/src/index.ts` — Montado contactRouter en /api/contact
- `e2e/cart.spec.js` — Fix URL pattern (producto → product)
- `e2e/shop.spec.js` — Fix URL pattern (producto → product)
- `e2e/full-flow.spec.js` — Fix selector de producto (usa #product-img-1)
- `e2e/product-detail.spec.js` — Fix precio (28000) y orden de talles (M, L, XL, XXL)

### Fase 5 ⏳ — Infraestructura de email real (pendiente)
- `server/src/services/emailService.ts` — transporter SMTP + sendWelcomeEmail, sendOrderConfirmationEmail con PDF, sendContactEmail real
- `server/src/services/emailTemplates.ts` — plantillas HTML con logo RMR
- Integrar en registro (bienvenida), compra (confirmación + PDF adjunto con pdfkit), y contacto (reenvío real al área)
- **Requiere**: `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM` en entorno

## Referencias
- Reglas y agentes: `AGENTS.md`
- Arquitectura: `docs/architecture/`
